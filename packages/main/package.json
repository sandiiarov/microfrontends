{
  "name": "main",
  "version": "1.0.0",
  "scripts": {
    "start": "NODE_ENV=development webpack-dev-server -d --port 9000",
    "build": "NODE_ENV=production webpack -p --progress",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "single-spa": "^4.0.1",
    "systemjs": "^2.0.2"
  },
  "devDependencies": {
    "externals": "^1.0.0",
    "webpack": "^4.20.2",
    "webpack-dev-server": "^3.1.9"
  }
}
